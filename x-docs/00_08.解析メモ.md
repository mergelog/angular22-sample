
---

- サーバから来るデータは必ず Store 経由。部品は input() で受け取るだけで、自分で取りに行かない。
- 並び順・フィルタ・表示列は URL が原本。操作 → Action → URL 書き換え → URL 監視 → 再取得、と一周する（詳細は 05-13 / 05-14）。

---

ブラウザのアドレスバーに表示されるURLと連携している
特に使われているのは、URLの ? 以降にある「クエリパラメータ」

対応関係は次のとおりです。
- order：並び順
- filter：列フィルタ
- columns：表示列


